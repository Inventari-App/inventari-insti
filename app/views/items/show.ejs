<%- include('../partials/detail', {
    type: 'item',
    item,
    features:
    {
        inventariable: localizeBoolean(item.inventariable),
        responsable: item.responsable.username,
        tipus: item.tipus,
        descripcio: item.descripcio,
    }
}) %>
