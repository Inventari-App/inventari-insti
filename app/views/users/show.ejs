<% layout('layouts/boilerplate')%>

<%- include('../partials/detail', {
    type: 'user',
    item: user,
    features: {
        usuari: user.username,
        nom: user.name,
        cognoms: user.surname,
        administrador: user.isAdmin,
        verificat: user.isVerified,
    }
}) %>
