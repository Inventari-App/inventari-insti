<% layout('layouts/boilerplate')%>

<%- include('../partials/detail', {
    type: 'user',
    item: user,
    features: {
        usuari: user.username,
        nom: user.name,
        cognoms: user.surname,
        administrador: localizeBoolean(user.isAdmin),
        verificat: localizeBoolean(user.isVerified),
        departament: user.department?.nom,
    }
}) %>
